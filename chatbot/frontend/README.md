# Chatbot Frontend — Placeholder

Not yet built. Per the phased build order in the approved plan, the web
chat widget (React + Vite, Entra ID SSO via Azure Static Web Apps) is
Phase 5, after the backend's parse→validate→PR→status loop is proven out.

Until then, `chatbot/backend`'s CLI (`npm run cli`) is the interim way to
exercise the parse/validate logic — see `chatbot/backend/README.md`.
