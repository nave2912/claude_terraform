{
  "name": "chatbot-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for the Terraform landing zone chatbot. Parses natural language into schema-valid JSON model changes; the infrastructure-change pipeline never runs terraform or talks to Azure directly, changes only ever land via a reviewed PR. src/observability is the one exception: read-only Azure Resource Manager calls (cost, activity log) for the Observability dashboard, never a write path.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "cli": "npm run build && node dist/cli/chat.js",
    "propose": "npm run build && node dist/cli/propose.js",
    "serve": "npm run build && node dist/api/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@azure/arm-costmanagement": "^1.0.0",
    "@azure/arm-resources": "^8.0.0",
    "@azure/arm-subscriptions": "^5.1.0",
    "@azure/identity": "^4.13.1",
    "ajv": "^8.17.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.5",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
