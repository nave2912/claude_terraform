name: chatbot-frontend-image

# Builds the chatbot frontend's Docker image and pushes it to the
# chatbot's own ACR (chatbotacrnaveenkumar, provisioned by
# environments/dev/main.tf's container_registry module). Publishing here
# means "pushed to ACR" — it does not update the running Container App;
# that's a separate step (bump container_image_tag / az containerapp
# update), same as the backend image.

on:
  push:
    branches: [main]
    paths:
      - "chatbot/frontend/**"
      - ".github/workflows/chatbot-frontend-image.yml"
  workflow_dispatch: {}

permissions:
  id-token: write # required for OIDC federated login to Azure
  contents: read

env:
  ACR_NAME: chatbotacrnaveenkumar
  IMAGE_NAME: chatbot-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_BACKEND_SUBSCRIPTION_ID }}
          ARM_USE_OIDC: true

      # admin_enabled = false on this registry by design (see
      # modules/container_registry) — this authenticates via the logged-in
      # identity's AAD token instead of an admin username/password. That
      # identity needs the "AcrPush" role on chatbotacrnaveenkumar; this is
      # a separate grant from the Terraform-apply identity's own roles and
      # is NOT provisioned by Terraform itself (RBAC-granting-RBAC is
      # exactly what environments/dev/main.tf's role assignments needed a
      # human to grant once, for the same reason).
      - name: az acr login
        run: az acr login --name "$ACR_NAME"

      - name: Build and push image
        run: |
          IMAGE="$ACR_NAME.azurecr.io/$IMAGE_NAME"
          docker build \
            -f chatbot/frontend/Dockerfile \
            -t "$IMAGE:latest" \
            -t "$IMAGE:${{ github.sha }}" \
            .
          docker push "$IMAGE:latest"
          docker push "$IMAGE:${{ github.sha }}"
